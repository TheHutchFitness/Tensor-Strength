# RCA — trainer-profiles-2 redeploy build failure (run 1a0d9440-35f9-482e-97d7-ab150a1f2830)

## Verdict
Build failed on a **source-level TypeScript compile error** in the Next.js `yarn build` step. This is an app-code fix, not a manifest/infra fix. Redeploying the same source will fail identically.

## The red herring
The error envelope's `log_excerpt` ends on step #0 `restore-pip-cache`:
`CommandException: No URLs matched: gs://emergent-default-customers-code/cache/trainer-profiles-2/pip-cache.tar.gz`
This is a **non-fatal cache miss** (a first-time / expired pip cache), not the cause. The build proceeded past it.

## The actual failure (build step #2 "docker-build", `RUN yarn build`)
```
./app/page.tsx:33:10
Type error: Cannot find name 'EmailCapture'.

 31 |         <MarketingExtras />
 32 |         <CustomProgram />
>33 |         <EmailCapture />
    |          ^
 34 |         <ContactForm />
 35 |       </main>
 36 |       <Footer />
Next.js build worker exited with code: 1
executor failed running [/bin/sh -c yarn build]: exit code: 1
```
Next.js 15.5.18 compiled successfully but failed the type-check phase: `app/page.tsx` line 33 renders `<EmailCapture />`, but the name `EmailCapture` is not in scope. The sibling components on the surrounding lines (`MarketingExtras`, `CustomProgram`, `ContactForm`, `Footer`) type-check fine, so only `EmailCapture` is unresolved.

## Root cause
`EmailCapture` is used but never imported/defined in `app/page.tsx`. Almost certainly a missing/removed import (e.g. component renamed, moved, or the import line dropped in the last edit).

## Fix (apply in the source environment)
In `app/page.tsx`, one of:
1. If an `EmailCapture` component exists in the codebase → add the correct import at the top, e.g.
   `import EmailCapture from "@/components/EmailCapture";` (adjust path/named-vs-default to match the actual file).
2. If the component was intentionally removed → delete the `<EmailCapture />` line (33).
3. If it was renamed → update the JSX usage to the new component name (and ensure that one is imported).

Verify locally with `yarn build` (or `next build`) before re-triggering — the type-check must pass.

## Note
Eph-pod reproduction could not be run this attempt (envcore sandbox returned repeated "container not found / deleted state" errors). The diagnosis above is taken directly and unambiguously from the Cloud Build log; no reproduction was needed to identify the exact file, line, and missing symbol.
