# RCA — Deploy run d9841c8d-dffe-40e3-9d33-e5d08b36b02f (trainer-profiles-2 / Tensor Strength)

## Outcome
Build failed at `build_image`. **Source-code fix required — not a manifest/infra/secret issue.** Not applied by me; E1 must fix in source and re-trigger the deploy.

## Failure
- Failed step: `build_image` → Cloud Build `765183a1-11e3-45cd-93da-bb69486efebf`, step #2 `docker-build` → `yarn build` exited 1.
- **Do NOT be misled by the error excerpt's tail**, which shows step #0 `restore-pip-cache`: `CommandException: No URLs matched: gs://.../trainer-profiles-2/pip-cache.tar.gz`. That is a **non-fatal pip-cache warm-miss** (no cache object yet) and is NOT the cause. The actual terminating error is the webpack compile failure below.

## Root cause (authoritative — from the compiler output)
`yarn build` failed to compile with a JSX **Syntax Error**:

```
Failed to compile.
./src/components/portal/ClientProgress.tsx
Error:   x Unexpected token. Did you mean `{'}'}` or `&rbrace;`?
   ,-[/app/src/components/portal/ClientProgress.tsx:75:1]
72 |             ))}
73 |           </div>
74 |         </div>
75 |       )}
   :       ^
76 |
77 |       {metrics.length > 0 && (
78 |         <div>
   `----
Caused by: Syntax Error
Import trace: ./src/components/portal/ClientProgress.tsx -> ./app/trainers/page.tsx
> Build failed because of webpack errors
```

The parser hits an **unbalanced JSX delimiter** at `src/components/portal/ClientProgress.tsx:75` — the `)}` on line 75 has no matching opening `(`/`{`. Given the surrounding structure (line 72 `))}` already closes a `.map(...)`/expression, followed by two closing `</div>`s), line 75's `)}` is a **stray/extra closing** — OR a wrapping element/fragment/opening paren above was dropped so the parser reaches line 75 expecting a different token.

## Fix (for E1 to apply in the source environment)
Open `src/components/portal/ClientProgress.tsx` and reconcile the JSX braces/parens around lines 70–78:
- Remove the stray `)}` on line 75, **or** restore the missing opening wrapper (`{cond && (` / fragment / `(`) that line 75 was meant to close, so the `(`…`)` and `{`…`}` counts balance across that block.
- After editing, verify locally with `yarn build` (or `yarn tsc --noEmit` / the linter) so the file compiles before redeploying. Also sanity-check the newly-added Phase-2/3/4 JSX in the same component tree, since this is a redeploy carrying new frontend work.

## Verification note
I could not reproduce in the eph sandbox — it failed to come up (`container not found` / `cannot exec in a deleted state`, infra-side). The diagnosis does not depend on a repro: the Next.js/SWC compiler prints the exact file, line (75), token, and import trace, which is conclusive for a syntax error.

## Next step
E1: apply the JSX fix in `src/components/portal/ClientProgress.tsx`, confirm `yarn build` passes, then re-trigger the deploy. No manifest change and no secret change is needed for this failure.
